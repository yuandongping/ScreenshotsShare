<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval" >
    <!-- 渐变 -->
    <gradient android:startColor="@color/theme_color"
        android:endColor="@color/bg_download"
        android:angle="270" />

    <!-- 描边 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/white" />

</shape>